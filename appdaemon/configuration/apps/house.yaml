everyone_gone:
  module: switches
  class: ToggleOnDeparture
  dependencies:
    - presence_app
  disabled_states:
    presence: vacation
  entities:
    switch: script.alle_weg
  properties:
    action_type: toggle
    state: "on"
